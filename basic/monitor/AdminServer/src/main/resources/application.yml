server:
  port: 10002
spring:
  application:
    name: AdminServer
  security:
    basic:
      enabled: true
    user:
      name: admin
      password: admin123

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:10000/eureka/